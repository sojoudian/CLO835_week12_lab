# Terraform
.terraform/
*.tfstate
*.tfstate.*
.terraform.lock.hcl
terraform.tfvars
*.tfplan
crash.log

# Keys / secrets — never commit these
*.pem
*.ppk
credentials

# Workshop 3 — generated client-cert artifacts (created on the master at runtime)
*.key
*.crt
*.csr
clo835_creds.sh

# OS
.DS_Store
